public class L2 {

     /*
     import java.io.FileWriter;
 import java.io.IOException;
 import java.io.Writer;
 public class FileLogger implements MessageLogger
 {
    private Writer writer;

    @Override
    public void logMessage(String message) {
    try {
    writer.write(java.time.LocalDateTime.now().toString() + " : ");
    writer.write(message + "\n");
    writer.flush();
    } catch (IOException e) {
    System.err.print("FileLogger failed to log : " + message);
    }
    }

    FileLogger(String fileName) throws IOException {
    writer = new FileWriter(fileName, true);
    }
 }




     ● Some commonly used methods, such as sort() rely on the Comparator
    interface, which is a functional interface
 ○ e.g. the following uses a Lambda expression to implement the Comparator interface
    orders.sort((orderA, orderB) -> orderA.amount() - orderB.amount());
*/
}
